class DataModel {
  String title, image, description, type;
  int price;

  DataModel(
      {required this.pric,
      required this.image,
      required this.description,
      required this.title,
      required this.type});
}
