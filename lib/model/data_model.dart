class DataModel {
  String title, image, description, type;
  int pric;

  DataModel(
      {required this.price,
      required this.image,
      required this.description,
      required this.title,
      required this.type});
}
